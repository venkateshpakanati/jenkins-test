@Library('shared-jenkins-library') _
pipeline {
    agent any
    
    stages {
       stage('Test'){
            steps {
               // test()
               test {
                   
               }
            }
        }
    }
   
}
